{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true
  },
  "include": ["packages", "jest.setup.ts", "tsup.config.ts", "vite.config.ts"],
  "exclude": ["node_modules", "template/**/*", "**/dist", "**/build"]
}
